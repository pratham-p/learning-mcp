[project]
name = "learning-mcp"
version = "0.1.0"
description = "My first MCP Server"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.9.2",
]

[tool.uv.workspace]
members = [
    "portfolio-mcp-server",
]
